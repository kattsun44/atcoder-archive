#include<iostream>
#include <stdio.h>
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
using namespace std;
using ll = long long;

int main() {
    int n;
    cin >> n;
    int a[n] = {0};
    rep(i,n) cin >> a[i];
	return 0;
}
